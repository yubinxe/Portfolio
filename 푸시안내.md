# GitHub에 올릴 파일 안내 (푸시 가이드)

GitHub Pages는 그대로 둡니다. 서버(백엔드) 없이 **정적 사이트에서 바로 작동**하도록 만들었습니다.
이 폴더의 파일을 레포 `yubinxe/Portfolio` 에 그대로 올리고 커밋하면 됩니다.

---

## 1. 올릴 파일 (이 폴더 = 그대로 커밋)

| 파일 | 상태 | 위치(레포 기준) |
| --- | --- | --- |
| `chatbot.js` | **새 파일** | 루트 |
| `index.html` | **교체** | 루트 |
| `career.html` | **교체** | 루트 |
| `gallery.html` | **교체** | 루트 |
| `Ethan Kim Portfolio.html` | **교체** | 루트 |
| `.gitignore` | 새 파일(있으면 병합) | 루트 |

> 4개 HTML은 `</body>` 바로 위에 `<script src="chatbot.js" defer></script>` 한 줄만 추가된 상태입니다. 나머지 내용·디자인은 원본 그대로입니다.
> `styles.css`, `sections.jsx`, `images/` 등 기존 파일은 **그대로 두면 됩니다**(안 건드렸습니다).

**❗ 절대 올리지 말 것: `.env` 파일** — 여기엔 API 키가 들어 있습니다. 이 푸시 폴더에는 `.env`를 넣지 않았으니, 이 폴더만 올리면 안전합니다. (GitHub 웹에 드래그로 올릴 때 `.env`를 실수로 포함하지 마세요.)

---

## 2. 커밋 방법

**방법 A — 본인 로컬 Portfolio 클론에 복사 후:**
```bash
git add -A
git commit -m "feat: 유빈 AI 포트폴리오 챗봇 추가"
git push origin main
```

**방법 B — GitHub 웹에서 직접:** 레포 → Add file → Upload files → 이 폴더의 파일 드래그 → Commit.
(단 `.env`는 올리지 마세요. 이 폴더엔 애초에 없습니다.)

푸시하면 GitHub Pages가 자동 갱신됩니다. 우측 하단에 챗봇 버튼이 나타납니다.

---

## 3. 키 적용 (지금 이대로 쓰는 방법)

정적 사이트라 키를 서버에 둘 수 없습니다. 대신 **당신 브라우저에만** 키를 저장해 GPT로 작동시킵니다.
배포된 사이트(`https://yubinxe.github.io/Portfolio/`)를 열고 **F12 → Console** 에 아래 한 줄:

```js
YubinChat.setKey("여기에_본인_OpenAI_키")
```

- 이 순간부터 **당신 브라우저에서** 챗봇이 GPT로 답합니다. (키는 localStorage에만 저장 — 레포·깃엔 안 올라감)
- 해제: `YubinChat.clearKey()` · 확인: `YubinChat.hasKey()`
- 키 없는 다른 방문자에게는 **포트폴리오 내장 지식**으로 답합니다(항상 작동, 지어내지 않음).

### 모든 방문자에게 GPT를 보이려면?
정적 사이트에서 그렇게 하려면 키를 코드에 넣어 공개해야 하는데 — **공개 레포에 키가 노출되면 수 분 내 도용되고 OpenAI가 자동 폐기**합니다(그래서 결국 작동하지 않음). 권장하지 않습니다.
정말 필요하면 **무료 프록시 1개**(Vercel/Cloudflare Workers)만 추가하면 키를 숨긴 채 전체 방문자에게 GPT를 제공할 수 있습니다. (이 폴더 밖 `api/chat.js`가 그 용도로 준비돼 있습니다. 원하시면 붙여드립니다.)

---

## 4. ⚠️ 보안 — 지금 이 키는 재발급하세요
현재 키는 대화창에 평문으로 공유되어 **유출된 것으로 간주**해야 합니다.
`platform.openai.com → API keys` 에서 기존 키 **Revoke** 후 새 키를 발급받아, 위 `setKey()` 에 새 키만 쓰세요.
